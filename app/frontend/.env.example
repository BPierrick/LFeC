# En développement : le client appelle directement le backend.
VITE_API_URL=http://localhost:5001
VITE_API_PORT=5173

# En production : NE PAS définir VITE_API_URL sur Vercel.
# Les requêtes /api sont proxifiées via vercel.json → Railway.